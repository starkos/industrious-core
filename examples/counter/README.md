# Industrious Core Counter Example

The canonical counter example used by all Flux and Redux implementations.

I've written it as a command line program, and kept it as simple as I could,
so you can focus on the library logic and not have to learn an unfamiliar 
UI toolkit first.

Just type `cargo run` to build and run it.

